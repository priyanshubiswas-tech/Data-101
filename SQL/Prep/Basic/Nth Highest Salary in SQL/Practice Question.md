## 🧠 SQL Practice Question 1: Second Highest Salary

Given the `Employees` table:

```sql
CREATE TABLE Employees (
  id INT,
  name VARCHAR(50),
  salary INT
);
```

### ❓ Task

Write a query to find the **second highest distinct salary** from the `Employees` table.
